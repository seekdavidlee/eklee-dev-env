# Setup alias for terraform for easy access
New-Alias -Name "tf" -Value "terraform" -Scope Global -Verbose

# Setup alias for kubectl for easy access
New-Alias -Name "k" -Value "kubectl" -Scope Global -Verbose